#pragma once
#include "stdafx.h"
#include "Header1.h"
template <typename T>
T MyABS(const std::vector<T>& b);

template <typename T>
std::istream& get(std::istream& is, MyMatrix<T>& M, std::vector<T>& b)